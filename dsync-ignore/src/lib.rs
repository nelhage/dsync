//! Ignore-rule parsing and translation for dsync.
//!
//! This crate will implement gitignore-semantics rule evaluation and the
//! translations to rsync filter rules and watchman query expressions
//! (Phase 5 of `doc/plan.md`). It is intentionally independent of the sync
//! and IPC code in the `dsync` crate.
//!
//! Currently a stub so the workspace builds.
